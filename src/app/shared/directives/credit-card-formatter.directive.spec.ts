import { CreditCardFormatterDirective } from './credit-card-formatter.directive';

describe('CreditCardFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new CreditCardFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
