import { CssAdvanceDirective } from './css-advance.directive';

describe('CssAdvanceDirective', () => {
  it('should create an instance', () => {
    const directive = new CssAdvanceDirective();
    expect(directive).toBeTruthy();
  });
});
